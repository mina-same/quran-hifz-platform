# Full URL of the backend API. On a physical device/simulator, "localhost" won't
# reach your dev machine — use your LAN IP (e.g. http://192.168.1.20:5000/api) or
# the deployed backend URL in production.
EXPO_PUBLIC_API_URL=http://localhost:5000/api
