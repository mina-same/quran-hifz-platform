# Physical device: must be the Mac's LAN IP — "localhost" resolves to the phone itself.
# Simulator can use either. Update if your LAN IP changes (ipconfig getifaddr en0).
EXPO_PUBLIC_API_URL=http://192.168.100.35:5001/api
